


/**
 * Creación de Arrays
 */
const numeros = [10, 20, 30, 40, 50, [1, 2, 3]];            //Creamos un Array (observamso que dentro tenemos otro Array)

console.log(numeros);                                       //Imprimimos el array entero

console.table(numeros);                                     //Lo imprimimos entero, pero en la consola del navegador nos lo muestar en una tabla


console.log(numeros[2]);                                    //Imprimimos lo que hay en la posición 2 del array

console.log(numeros[5][1]);                                 //Imprimimos el array que esta en la posición 5, y dentro de este, lo que hay en su posición 1
